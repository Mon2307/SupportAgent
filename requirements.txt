langchain==0.2.2
langchain-community==0.2.3
langchain-aws # BedrockEmbeddings (Titan) used by create_database.py
langchain-chroma # Chroma vector store wrapper
langchain-text-splitters # RecursiveCharacterTextSplitter
langchain-core
unstructured==0.14.4 # Markdown loading via DirectoryLoader
chromadb==0.5.0 # Vector storage
boto3==1.34.117 # Bedrock (Titan embeddings)

numpy==1.26.4 # pinned: chromadb depends on it transitively, no prebuilt NumPy 2.x wheel everywhere
huggingface-hub==0.24.6 # pinned: chromadb -> tokenizers -> huggingface-hub; newer versions need hf-xet (Rust build)

# install markdown parsing deps with: pip install "unstructured[md]"
